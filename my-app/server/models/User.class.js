class User {
  constructor(id, password, firstName, lastName, isAdmin) {
    this.id = id;
    this.firstName = firstName;
    this.lastName = lastName;
    this.password = password;
    this.isAdmin = isAdmin;
  }
}
